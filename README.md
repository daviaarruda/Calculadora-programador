# 🧮 Calculadora Programador Didática (Assembly MIPS)

Este projeto implementa uma **calculadora programador didática** em Assembly MIPS, que mostra **passo a passo** como ocorre a conversão de um número decimal para:

- **Base 2 (binário)**
- **Base 8 (octal)**
- **Base 16 **

---

## ⚙️ Requisitos
🔗 Baixe
- **MARS 4.5** (MIPS Assembler and Runtime Simulator)  

---

## 🚀 Como rodar o programa

1. **Abra o MARS 4.5**  
   Execute o arquivo `MARS.jar`.

2. **Carregue o código Assembly**  
   Vá em **File → Open** e selecione o arquivo `.asm` da calculadora.

3. **Monte o programa**  
   Clique em **Assemble** (ou pressione `F3`).

4. **Execute o programa**  
   Clique em **Run → Go** (ou pressione `F5`).
